#!/usr/bin/env bash
#were multiplying now
i=1
while [ "$i" -le 101 ]
do
	if [[ $((i%15)) == 0 ]]; then 
	       echo "FizzBuzz"
       elif [[ $((i%3)) == 0 ]]; then 
	       echo "Fizz"
       elif [[ $((i%3)) == 0 ]]; then 
	       echo "Buzz"
	fi
	i=$(($i+1))
	echo "$i"
done	

